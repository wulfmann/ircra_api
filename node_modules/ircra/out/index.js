'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
var gradeMap_1 = require("./gradeMap");
exports.default = (function (grade, system, desiredGrade) {
    var errors = [];
    if (!grade)
        errors.push('grade');
    if (!system)
        errors.push('system');
    if (errors.length > 0) {
        throw new Error(errors.join(' and ') + " are required.");
    }
    var gradeSet = gradeMap_1.grades.find(function (gradeSet) { return gradeSet[system] === grade; });
    if (gradeSet) {
        if (desiredGrade)
            return gradeSet[desiredGrade];
        return gradeSet;
    }
});
